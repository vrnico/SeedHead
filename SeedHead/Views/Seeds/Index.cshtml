@model IEnumerable<SeedHead.Models.Seed>

<html>
<head>
    <title>SeedHead</title>
</head>
<body>
    <h2>SeedHead</h2>
    <h4>@Html.DisplayNameFor(model => model.Description)</h4>
    <ul>
        @foreach (var listItem in Model)
        {
            <li>@Html.DisplayFor(modelItem => listItem.Description)</li>
        }
    </ul>
</body>
</html>